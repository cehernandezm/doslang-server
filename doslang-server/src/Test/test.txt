Program Hola;


function modulo(m:integer): Integer;
Var
modulo : Integer;

function factorial(fac:integer):Integer;
Var
factorial : Integer;
BEGIN

	if(fac <= 0 ) THEN factorial := 1;
	else factorial := factorial(fac - 1) * fac;

END;

BEGIN
	modulo := factorial(m);
END;


BEGIN
Writeln(modulo(2));

END.
